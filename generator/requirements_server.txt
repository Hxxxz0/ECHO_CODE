# Server-specific dependencies for WebSocket inference service
# Install with: pip install -r requirements_server.txt

# Core server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Note: The following dependencies should already be installed from requirements.txt:
# - torch>=1.10.0
# - numpy
# - accelerate
# 
# If running the server in a separate environment, also install:
# pip install -r requirements.txt



