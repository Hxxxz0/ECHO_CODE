# Core
torch
numpy
scipy
pyyaml
einops
tqdm
Pillow
accelerate
diffusers>=0.21.0
matplotlib
git+https://github.com/openai/CLIP.git

# Training / eval (optional)
tensorboard
transformers

# For WebSocket server, also install:
# pip install -r requirements_server.txt
