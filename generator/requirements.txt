# Core dependencies
torch
tqdm
scipy
numpy
matplotlib>=3.7.0
accelerate
transformers
einops
diffusers>=0.21.0,<0.30.0
git+https://github.com/openai/CLIP.git

# Training
tensorboard

# For WebSocket deployment server, also install:
# pip install -r requirements_server.txt
